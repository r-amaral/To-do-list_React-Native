import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    headerContainer: {
        marginTop: 45, marginLeft: 20
    },
    headerImage: {
        width: 70,
        height: 70,
        borderRadius: 100
    },
    headerText: {
        fontSize: 16,
        color: '#FFFFFF',
        marginTop: 10
    }
})