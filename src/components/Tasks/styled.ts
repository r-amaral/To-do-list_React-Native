import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        borderWidth: 1.5,
        borderColor: 'white',
        borderRadius: 20,
        borderStyle: 'solid',
        flexDirection: 'row',
        alignItems: 'center',
        padding: 10,
    },
    hour: {
        color: 'white',
    },
    description: {
        color: 'white',
        marginTop: 20,
    },
});